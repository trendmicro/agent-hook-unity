{
  "name": "agent-hook-spec",
  "private": true,
  "version": "0.0.0",
  "description": "The community home for the Agent Hook Spec.",
  "scripts": {
    "test": "node --test scripts/validate.test.mjs",
    "validate": "npm test && node scripts/validate.mjs && node scripts/validate-image-size-patch.mjs",
    "build": "npm --prefix website run build",
    "start": "npm --prefix website run start"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  },
  "engines": {
    "node": ">=20"
  }
}
